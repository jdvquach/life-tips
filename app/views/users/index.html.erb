<div>
<h2>Users</h2>

<ul>
<% @users.each do |user| %>
  <li>

      <!-- <%= user.name  %>, (<%= user.email %>) -->
      <%= link_to user.name, user_path(user.id) %>, <%= user.email %>

  </li>
<% end %>
</ul>
</div>
